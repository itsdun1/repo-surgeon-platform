apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None  # headless — direct pod DNS for StatefulSet
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: pg
      port: 5432
      targetPort: pg
